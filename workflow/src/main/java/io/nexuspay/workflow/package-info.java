/**
 * Workflow bounded context (Phase 4).
 * No-code workflow builder, routing rule engine, conditional logic, A/B test framework.
 */
@org.springframework.modulith.ApplicationModule(
        allowedDependencies = {"common", "payment"}
)
package io.nexuspay.workflow;
