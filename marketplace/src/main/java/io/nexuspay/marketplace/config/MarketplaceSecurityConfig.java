package io.nexuspay.marketplace.config;

import org.springframework.context.annotation.Configuration;

/**
 * Marketplace security configuration placeholder.
 * PCI isolation and API key scoping for connected accounts will be implemented
 * when the marketplace module is extracted to a standalone service.
 *
 * @since 0.4.1 (Sprint 4.2)
 */
@Configuration
public class MarketplaceSecurityConfig {
    // Connected account API key scoping and rate limiting will be added here
}
