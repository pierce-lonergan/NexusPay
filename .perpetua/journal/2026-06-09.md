# Journal — 2026-06-09 — session 0 (bootstrap)

## Context
Repo did not compile or run. A full 6-agent audit had already been performed and
~90 files of fixes made (uncommitted) taking it to a green build + booting app.
This session bootstraps PERPETUA and runs the first improvement iterations.

## BOOT
- No STOP. Branch was `main`, dirty (90 paths). Created `perpetua/bootstrap`.
- Reconciled git: HEAD 6281809; remote pierce-lonergan/NexusPay (will NOT push).
- No prior `.perpetua/` or `CLAUDE.md` — clean bootstrap.

## Actions
- Committed the audit-fix body as `4a1c6ea` (preserve work, Prime Directive 4).
- Scaffolded `.perpetua/` per §14: CHARTER (draft, awaiting ratify), ratchets
  (measured reality — 201 tests; coverage/mutation honestly UNMEASURED),
  BACKLOG (14 scored items from deferred audit findings), MAP, LESSONS (14
  entries), THREAT_MODEL, AUDITS, DECISIONS (5 ADRs), QUESTIONS (4), ROADMAP,
  IDEAS, METRICS, DIGEST.
- CLAUDE.md operating core; copied PERPETUA.md + perpetua-loop.sh into repo;
  added CI gate workflow; gitignored runtime + .claude.

## Baseline honesty notes
- Coverage/mutation NOT measured (no JaCoCo/PIT). Floors are placeholders, not
  achievements — flagged in ratchets + B-005.
- No automated security scan run yet (B-006). HIGH findings are from manual
  review, tracked in AUDITS + BACKLOG.
- Integration tests + real boot need Docker (absent here) — Q-004.

## Iterations
- **Iter 1 — B-010 (T2, settlement-ingest jsonb)**: entity `@JdbcTypeCode(SqlTypes.JSON)`
  + StripeCsvParser emits `{"raw":line}`. 4 tests (reconciliation had none). Adversarial
  review SHIP (fixed an inaccurate code comment; deferred Testcontainers round-trip → B-016,
  RFC-4180 CSV → B-015). Commit 6e8cf67.
- **Iter 2 — B-001 (T3, billing scheduler double-billing)**: `SchedulerLock` (Valkey
  SET-NX-EX, fail-closed, self-renewing lease, atomic Lua release, reentrancy guard) on
  3 crons. 10 tests. Adversarial review REJECTED v1 (fixed-TTL could expire mid-run over a
  ≤500-charge loop → double-charge; verified new-invoice-per-cycle defeats PSP idempotency)
  → hardened with lease renewal. Security review SHIP. ADR-006; threat model B-001 PARTIALLY
  CLOSED; follow-ups B-017/B-018. Commit dd2a6e6.

## Iter 3 — B-019 (token-aware adaptive scheduling) [human-directed]
DESIGN: human asked to maximize productive Fable-5 token usage within the 5h rolling
window while away — fill the window, don't leave quota unspent, don't slam the cap early.
Approach: (a) `scripts/perpetua-pace.sh` pure pacing controller (deadband: behind pace→
AGGRESSIVE/gap 0; ahead→COOLDOWN/sleep to re-cross line; near cap→BLOCKED/wake at block end;
else STEADY); (b) `scripts/perpetua-usage.sh` ccusage wrapper → one-line budget hint +
machine-readable runtime/budget-state; (c) wire both into perpetua-loop.sh (dynamic gap +
hint carries decision); (d) CLAUDE.md "rigor dial" so the agent scales DEPTH (reviewers,
audit breadth, research, mutation) to the budget signal — productive burn, never churn.
Files: scripts/perpetua-pace.sh (new), scripts/perpetua-usage.sh (new),
scripts/test-perpetua-pacing.sh (new), perpetua-loop.sh (edit), CLAUDE.md (edit), ADR-007.
Test plan: pure pace functions unit-tested via a bash assertion script (synthetic U/B/elapsed);
bash -n on the harness; graceful degradation when ccusage absent (fall back to existing hint).
Tier: T2 (orchestration tooling; not a money danger-zone). Pre-mortem: if the ccusage estimate
or cap is wrong the controller mis-paces — but Anthropic's server-side 5h/weekly caps are the
hard backstop, so worst case is sub-optimal pacing (early block or some idle), never overspend.

## SHUTDOWN (session 0)
- Full suite: 202 executed pass / 13 Docker-skip / 0 fail; build green.
- Honesty correction: ratchet test_count_floor was asserted (201), not measured →
  corrected to true 202 after a clean full run.
- Did NOT push / open PRs (L1, Q-001 pending). Did NOT execute perpetua-loop.sh
  (it spawns claude -p recursively); syntax-checked only + documented WSL2/Task-Scheduler
  scheduling.
- Next session: B-004 (secrets fail-fast) then B-006 (baseline scans). Meta-review due at
  session 10; deep audit at session 7.
