# LESSONS — root-cause classes & guardrails

Format: `L-NNN | date | class | bug | guardrail | link`

L-001 | 2026-06-09 | money/cross-currency-aggregation | Journal entry "balanced" check summed minor units across currencies, accepting +10000 JPY / −10000 USD as balanced | Per-currency zero-sum in `JournalEntry`; regression test `JournalEntryTest`; GUARDRAIL TODO: DB CHECK constraint (B backlog) | 4a1c6ea
L-002 | 2026-06-09 | persistence/no-op-optimistic-lock | Bulk JPQL balance UPDATE checked `version` but never incremented it → lost updates undetectable | UPDATE now `SET version = version+1`; `@Modifying(clearAutomatically,flushAutomatically)` | 4a1c6ea
L-003 | 2026-06-09 | identifier-convention-drift | Two different account-id conventions (`la_merchant_recv_` vs slug) → every non-USD payment dead-lettered | Single canonical `EnsureAccountsExistUseCase.accountId()` + typed helpers used everywhere | 4a1c6ea
L-004 | 2026-06-09 | idempotency/wall-clock-key | Billing idempotency key embedded `System.currentTimeMillis()` → every retry a fresh key → double charges | Deterministic key per (invoice, logical attempt); GUARDRAIL TODO: lint for currentTimeMillis in idempotency keys | 4a1c6ea
L-005 | 2026-06-09 | fraud/score-polarity | Signifyd TRUST score (higher=safer) used directly as RISK score → approved fraud, blocked good orders | Inverted mapping with documented polarity; GUARDRAIL TODO: provider-scale unit tests (B-014) | 4a1c6ea
L-006 | 2026-06-09 | money/currency-exponent | FRM amount conversions hardcoded ÷100 / ×10000 → 10–100× wrong for JPY/BHD | `FrmAmounts` derives factor from `Currency.getDefaultFractionDigits()` | 4a1c6ea
L-007 | 2026-06-09 | security/non-constant-time-compare | Webhook HMAC compared with `String.equalsIgnoreCase` (timing side-channel) | `MessageDigest.isEqual`; GUARDRAIL TODO: semgrep rule for HMAC string compare | 4a1c6ea
L-008 | 2026-06-09 | security/path-traversal | Evidence storage joined attacker-controlled filename into a path | Sanitize each segment + `normalize().startsWith(root)` check | 4a1c6ea
L-009 | 2026-06-09 | pci/unkeyed-fingerprint | PAN fingerprint was unkeyed SHA-256 (brute-forceable low-entropy PAN) | HMAC-SHA256 keyed from master key; store 6-digit BIN not 8 | 4a1c6ea
L-010 | 2026-06-09 | security/cross-tenant-cache-key | Idempotency cache key was the raw client header → cross-merchant response leakage; 5xx cached 24h | Scope key per caller credential hash; never cache 5xx | 4a1c6ea
L-011 | 2026-06-09 | startup/component-scan-root | @SpringBootApplication under io.nexuspay.app hid sibling modules' entities + all Modulith modules | Moved to io.nexuspay root; ModulithVerificationTest now meaningful | 4a1c6ea
L-012 | 2026-06-09 | startup/unimplemented-port | Port interfaces injected into @Service beans with no implementation → context fails (reconciliation, dispute) | Implemented adapters; GUARDRAIL: a context-load smoke test would catch this class (IDEAS) | 4a1c6ea
L-013 | 2026-06-09 | startup/bean-cycle | DataSource-decorating @Bean that consumed a DataSource created a Flyway↔datasource cycle | Decorate via BeanPostProcessor (no new bean edge) | 4a1c6ea
L-014 | 2026-06-09 | outbox/ack-ordering | Relay marked events published before Kafka ack → events lost on broker outage | Await send ack before markPublished | 4a1c6ea

L-020 | 2026-06-10 | repo-health/clean-clone-unbuildable | A clean clone (CI/new contributor) could not build: gradlew lost its +x bit (exit 126) AND gradle-wrapper.jar was excluded by a `*.jar` gitignore (ClassNotFoundException GradleWrapperMain) | committed the wrapper jar (gitignore negation after *.jar) + git update-index --chmod=+x; GUARDRAIL: CI build job is the standing check (only CI caught both) | CI run on PR #1
L-021 | 2026-06-10 | deps/testcontainers-commons-compress | Integration tests (first CI run) died with ExceptionInInitializerError in commons-compress TarArchiveOutputStream — Testcontainers 1.19.8 needs commons-compress 1.24.0 but Boot 3.2.5 BOM pulls 1.25.0 | force 1.24.0 (test-only); lesson: pin TC's commons-compress when on a newer Boot BOM | PR #1
L-022 | 2026-06-10 | deps/flyway-major-vs-boot | AbstractMethodError in FlywayExecutor — catalog pinned Flyway 10.15.0 + flyway-database-postgresql (a Flyway-10 artifact, unresolvable under Boot 3.2.5's Flyway-9 BOM) run against Boot's Flyway-9 autoconfig | use Boot-managed flyway-core 9.22.3 (Postgres support built in), drop flyway-database-postgresql; GUARDRAIL: don't pin a major Flyway above what the Boot BOM supports | PR #1
- (root-cause class "deps/*" now 2× (L-021/22), "repo-health/*" 1× — all CI-surfaced; reinforces that pushing to CI is the verifier for packaging/dep issues local Windows can't catch)
L-019 | 2026-06-10 | money/minor-units-x-major-rate-no-exponent | FX convert multiplied raw ISO-4217 MINOR units by a MAJOR-unit rate with no exponent handling → USD→JPY 100× off, BHD 10× — found in 3 sites (FxRate, FxRateLock, CurrencyConversion.fxGainLoss); SAME class as L-006 (FrmAmounts hardcoded /100) → RECURRING, meta-review should add a systemic guardrail | Single exponent-aware `CurrencyMath` helper all sites delegate to + 11 tests; GUARDRAIL TODO: arch/grep test forbidding `.multiply(rate` on raw minor units | B-014a
L-018 | 2026-06-10 | money/maker-checker-not-execute-once | Approved refund executed with a null idempotency key via a read-then-write approve that ignored tenant → duplicate/cross-tenant refund under retry or concurrent approve | Atomic conditional UPDATE (WHERE status='PENDING' AND tenant_id=?, rows==1) + deterministic idempotency key from approval id + tenant-ownership check; ApprovalServiceTest/RefundOrchestrationServiceTest. Residual stuck-APPROVED recovery → B-022 | B-009
L-017 | 2026-06-10 | security/dev-default-secret-in-prod | Session HMAC key, webhook secret, vault master key defaulted to committed dev values with no prod guard — a missing env var would sign/encrypt with a public key | StartupSecretsValidator fail-fast under prod profile + application-production.yml forcing it + a drift-guard test asserting KNOWN_DEFAULTS stays in sync with application.yml (control can't fail open) | B-004
L-016 | 2026-06-10 | concurrency/lease-shorter-than-work | A fixed-TTL distributed lock over an unbounded long-running (≤500 PSP charges) job can expire mid-run → second replica starts → double-charge (a new invoice per cycle means no downstream idempotency saves it) | Lease renewal at ttl/3 while work runs + atomic owner-checked Lua release; SchedulerLockTest; caught by adversarial review, not initial tests | B-001/ADR-006
L-015 | 2026-06-10 | persistence/jsonb-as-varchar | `settlement_records.raw_data` jsonb column mapped as a plain String (no @JdbcTypeCode) → every ingest INSERT aborts; parser also stored a non-JSON CSV line | @JdbcTypeCode(SqlTypes.JSON) + parser emits valid JSON; StripeCsvParserTest; GUARDRAIL TODO: a grep/arch test for `columnDefinition="jsonb"` String fields lacking the annotation | B-010
- (root-cause class "persistence/*" now 2× with L-002 — meta-review watch)

RECURRING-CLASS WATCH (for meta-review): "startup/*" appears 3× (L-011/12/13) — a
CI context-load smoke test (no external infra, mocked) is the systemic guardrail.
"money/*" appears 3× (L-001/04/06) — mutation testing on ledger/billing/fraud is
the systemic guardrail (B-005/B-014).

## B-011 saga — the full integration-test bring-up (2026-06-10)
META-LESSON (the big one): the 13 integration tests had NEVER run since they were
written — Flyway's cross-module V1 collision aborted the schema before any test
could boot the context. So ~10 layers of schema↔entity drift and wiring bugs
accumulated UNVERIFIED behind that one blocker. Clearing it made CI surface them
one boot-layer at a time (resolver → discovery → DDL → validate → bean wiring →
external deps → security → assertions). GUARDRAIL (now standing): CI runs the full
context against real Postgres/Kafka/Redis (Testcontainers) and the integration
suite is GREEN — this is the systemic guard L-011/12/13 called for, now real. Any
future drift in this class fails CI immediately. Discipline reaffirmed: do NOT
delete load-bearing infra on an assumption (I deleted the 13 Flyway customizers
believing the base scan was redundant — it silently dropped 11 modules; L-013/§13).

L-023 | 2026-06-10 | db/flyway-location-semantics | (a) bare `classpath:db/migration` recursive scan is unreliable across separate module JARs; (b) a profile yaml `locations` list REPLACES (not merges) the base, so application-test.yml's hand-maintained 4-leaf list silently dropped the `app` leaf (current_tenant_id) → every RLS policy after it failed | explicit per-module LEAF locations + `fail-on-missing-locations: true`; test profile inherits the full list (no override) so test schema == prod | B-011
L-024 | 2026-06-10 | db/rls-policy-references-missing-column | RLS policies on child tables (split_rules, webhook_triggers) compared a `tenant_id` column the table doesn't have (tenant lives on the parent) → 42703 once migrations finally ran | derive tenant from the parent via correlated subquery; GUARDRAIL: a CI parser asserting every `USING (tenant_id` policy's table has the column | B-011
L-025 | 2026-06-10 | jpa/hibernate-validate-drift | ddl-auto=validate surfaced mass schema↔entity drift never caught before: Spring Modulith event_publication table+event_type missing (JPA variant ships no DDL); 23 jsonb String columns lacked @JdbcTypeCode(JSON) (also breaks INSERT bind); List/Map fields map as JSON not native array (events text[]→jsonb); Double↔DECIMAL | reconcile via the entity (javap for libs); a static entity↔migration type checker batches the whole class instead of 1-CI-cycle-each; GUARDRAIL: validate in CI is now the standing check | B-011
L-026 | 2026-06-10 | jpa/nested-repositories-not-scanned | 4 out-adapters declared Spring Data interfaces as NESTED types; default Boot scan uses considerNestedRepositories=false → NoSuchBeanDefinitionException, never seen because the context never booted | @EnableJpaRepositories(considerNestedRepositories=true) on the app class (defaults preserve EMF/TM + base package) | B-011
L-027 | 2026-06-10 | rls/set-command-no-bind-params | `SET LOCAL app.current_tenant_id = ?` as a PreparedStatement is a syntax error — PostgreSQL SET takes no bind parameters; broke every connection checkout with a tenant set | use `SELECT set_config('app.current_tenant_id', ?, true)`; deeper B-002 (set runs pre-transaction at getConnection; test user is RLS-exempt owner) still tracked | B-011/B-002
L-028 | 2026-06-10 | web/security-exception-as-500 | a @ControllerAdvice catch-all @ExceptionHandler(Exception) caught Spring Security's AccessDeniedException before ExceptionTranslationFilter → authorization failures leaked as 500 instead of 403 | explicit @ExceptionHandler(AccessDeniedException)->403; GUARDRAIL: integration tests asserting 403 for unauthorized roles (now green) | B-011
L-029 | 2026-06-10 | health/downstream-drags-aggregate | external-dependency health indicators (HyperSwitch PSP, Keycloak) as plain @Component connection-refuse with no backend → aggregate /actuator/health = DOWN (503) in any env lacking them | make such indicators @ConditionalOnEnabledHealthIndicator (default-on) so they're disableable; off in the test profile; (consider moving downstream checks to the readiness group in prod) | B-011
L-030 | 2026-06-10 | test/external-deps-unguarded | full-context integration tests tried to reach Vault (token), Keycloak, PSP that aren't in the container set; some were guarded (@ConditionalOnProperty Temporal/Vault-config) but others weren't | disable/guard every external integration in the test profile (spring.cloud.vault.enabled=false, health indicators off); pattern: external clients must be conditional so a no-backend env degrades cleanly | B-011
L-031 | 2026-06-10 | security/wiring-is-not-enforcement | Wiring a built-but-unwired control (B-003 fraud+sanctions gate) made it LOOK done, but the T3 review found it was bypassable: covered only 1 of ≥3 PSP entrypoints (not confirm/capture or billing/b2b/workflow callers), drove sanctions off client-supplied metadata an attacker can omit/forge, and sat on an OFAC adapter whose CSV parser silently produced an empty list (→ 4-country static fallback, fail-open). Recurring bug class: a control is only as strong as its WEAKEST entrypoint + its most-attacker-controlled INPUT + its failure DIRECTION (default-allow = broken sanctions). | GUARDRAIL: for any security gate, the review must enumerate (a) every entrypoint to the protected action and confirm each is gated, (b) whether each decision input is server-authoritative or client-controlled, (c) the fail direction (closed for sanctions). Don't overclaim coverage in commit/docs; an honest first-cut + tracked gaps (B-024..B-028, ADR-009) beats a "done" that's bypassable. | B-003
L-032 | 2026-06-10 | security/uniform-control-on-non-uniform-flows | Moving the fraud gate to the PaymentGatewayPort boundary (B-024) made it screen ALL callers uniformly — but a "REVIEW → hold capture" decision that is right for an INTERACTIVE checkout is WRONG for a server-initiated recurring billing charge: holding it returns requires_capture, which billing reads as a payment failure and DUNS the customer (a live regression). T3 review (M1) caught it. Lesson: when one control spans heterogeneous flows (interactive vs pre-authorized/recurring vs batch), the ENFORCEMENT ACTION must be flow-aware, not just the decision — same screen, different consequence. | GUARDRAIL: hold-capture is INTERACTIVE-only; server rails capture + flag for analyst review (sanctions still hard-block). When a port-boundary decorator changes behavior for a previously-unscreened caller, trace that caller's success/failure interpretation (here billing's PaymentResult→boolean→dunning) before shipping. | B-024
L-033 | 2026-06-13 | test/new-profile-trips-prod-secrets-guard | The RlsEnforceIT (which activates the `rls-enforce` Spring profile) failed to boot in CI: StartupSecretsValidator (B-004) refused to start because it classifies ANY unrecognized non-dev profile as production (fail-safe), so the built-in DEV default secrets were "in effect under a production profile". The GUC mechanism under test was never the failure — the context never finished booting. | GUARDRAIL: when a test (or any runtime) activates a NON-dev profile, it inherits production-grade guards. Fix the TEST (supply non-default managed secrets via @DynamicPropertySource — jwt ≥32 bytes for HMAC, valid-Base64 vault key, non-default webhook secret), NEVER weaken the control or bake test secrets into the shared profile yaml (that profile is also the real prod-cutover profile and must keep failing closed). Read a failing security guard's exact trigger before "fixing" it. | B-002/B-004
L-034 | 2026-06-13 | aop/role-pin-is-threadlocal-not-tx | For cross-tenant jobs, @SystemTransactional is acted on by an @Around aspect that wraps the call in DbRoleContext.runAs(SYSTEM,…) — a CALL-SCOPED thread-local the routing datasource reads on every getConnection. Two consequences that drive WHERE you annotate: (a) annotate the JOB ENTRY method (the @Scheduled tick); the pin then covers the whole SYNCHRONOUS subtree — nested @Transactional service calls, self-invocation, and even non-transactional reads all route to the system pool — so you need NOT chase the inner tx boundary; (b) it does NOT cross thread boundaries: async callbacks (e.g. Kafka producer whenComplete), @Async, and executor work run on the DEFAULT (APP) role and would fail-closed if they touch RLS tables. | GUARDRAIL: put @SystemTransactional on the entry method; for any work the job hands to another thread, either keep it off RLS tables or give it its own SYSTEM-pinned path. Verify the aspect order pins SYSTEM (HIGHEST_PRECEDENCE) before the TransactionInterceptor (LOWEST). | B-002
L-035 | 2026-06-13 | security/system-bypass-forfeits-with-check | Classifying a cross-tenant background job as SYSTEM (BYPASSRLS) is correct for the all-tenant DISCOVERY scan (a per-tenant loop can't see which tenants have due work), but BYPASSRLS also forfeits the WITH CHECK on the per-item WRITES — a poisoned/aliased row could persist a child (invoice line / outbox event / dunning attempt) under the wrong tenant with no DB backstop. The adversarial pass flagged this on RenewalScheduler + TrialManagementService; the post-ship review then settled BOTH the same way — annotate the discovery sweep @SystemTransactional (TrialExpirationScheduler was initially missed → fixed, making 16 SYSTEM jobs), with per-item tenant binding on the writes deferred to B-002-activation-tenant. Two independent agents gave structurally-identical code different verdicts (SYSTEM vs TENANT_SCOPED) — a reminder to reconcile sibling-job classifications explicitly. | GUARDRAIL: for an all-tenant sweep, run the DISCOVERY read as SYSTEM but bind TenantContext to each row's tenant for its per-item write transaction (split the one cross-tenant tx into per-tenant txns; one app-role tx GUC is tx-local and single-valued). A Kafka consumer carrying a tenantId is always TENANT_SCOPED, never SYSTEM. Tracked as B-002-activation-tenant. | B-002
L-036 | 2026-06-14 | tx/helper-tx-downgrades-inner-isolation | A generic "run this in a tenant-bound REQUIRES_NEW transaction" helper (TenantWorkRunner.runInTenant, default isolation) SILENTLY downgrades an inner @Transactional(SERIALIZABLE) method that JOINS it (PROPAGATION_REQUIRED): Spring does not escalate an existing tx's isolation and, with validateExistingTransaction=false (the default), does not even error — so the ledger's SERIALIZABLE journal write quietly ran at READ_COMMITTED. Worse, this happened in BOTH modes, so it was also a dormancy break at enforce=false. | GUARDRAIL: when the inner work already manages its own transaction(s)/isolation, BIND the tenant WITHOUT opening an enclosing tx (added TenantWorkRunner.bindTenant — set APP role + TenantContext on the thread, let the inner @Transactional open its own tx at its own isolation). Only use runInTenant/callInTenant when YOU own the boundary (handlers that aren't themselves transactional, or per-item sweep writes). Audit every helper-wrapped call for an inner non-default isolation. | B-002-activation-tenant
L-037 | 2026-06-14 | rls/app-level-filter-is-not-gated-by-the-guc | Swapping a repository finder (findAllByEnabledTrue → findAllByTenantIdAndEnabledTrue) to "add RLS scoping" added an UNCONDITIONAL SQL `WHERE tenant_id=?`. That filter is application-level — it is NOT gated by rls.enforce or the GUC — so it changed behavior at enforce=false too: the event tenant resolves to "default" (no tenant on the wire pre-cutover) while endpoints are stored under real tenants → zero matches → webhooks silently stopped in the DEFAULT config. The dormancy guarantee only covers the GUC/role machinery (the inline helper ignores the tenant); it does NOT cover app-level query changes. | GUARDRAIL: a behavior change that must be inert until cutover has to be gated on the enforce FLAG (inject nexuspay.multi-tenancy.rls.enforce and branch), not merely left to the dormant GUC. Any query/method swap is a behavior change — prove the enforce=false path is byte-identical (a regression test or explicit branch). Caught by the adversarial review, not by the bean-wiring dormancy IT. | B-002-activation-tenant
L-038 | 2026-06-14 | ci/which-workflow-is-green | I reported "CI green" for branch commits when only perpetua-gates (changelog/secrets/ratchet) had run — the build+test CI workflow (ci.yml) triggers on push:[main] + pull_request:[main], so branch pushes only got build+test while PR #1 was OPEN. Merging PR #1 silently stopped branch build+test; the next pushed increments were never compiled or IT-run, yet a green perpetua-gates looked like success. | GUARDRAIL: when claiming CI green, confirm WHICH workflow(s) ran (`gh run list` → check the name is the build+test one), not just that "a run" is green. Process fix applied: ci.yml now also runs on push:[perpetua/**] so branch pushes are always compiled+IT-run regardless of PR state. Report verification faithfully — name the workflow. | meta
L-039 | 2026-06-14 | tx/durable-error-record-erased-by-outer-rollback | The B-015 fix persisted a PARSE_ERROR exception for each bad settlement row so money never vanishes silently — BUT it saved them inside the caller's @Transactional (REQUIRES default) boundary (runFromUpload). When anything downstream (matching, a DB error) throws, the orchestrator marks the SHARED transaction rollback-only and re-throws, so the just-persisted error records roll back too — re-creating the exact silent-vanish defect, relocated from the parser to the tx boundary. Unit tests with a MOCKED repo did NOT catch it (a Mockito interaction is recorded regardless of rollback). Caught by adversarial review tracing the real wired flow. | GUARDRAIL: a record whose PURPOSE is to survive a failure of the surrounding work (audit log, dead-letter, parse/validation exception, reconciliation exception) MUST be written in its OWN committed transaction — @Transactional(propagation = REQUIRES_NEW) on a SEPARATE bean (not self-invoked) — never in the same tx as the work that may roll back. When testing durability, exercise the real tx boundary (a rollback after the write), not a mock that ignores rollback. | B-015
