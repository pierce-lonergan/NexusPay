# LESSONS — root-cause classes & guardrails

Format: `L-NNN | date | class | bug | guardrail | link`

L-001 | 2026-06-09 | money/cross-currency-aggregation | Journal entry "balanced" check summed minor units across currencies, accepting +10000 JPY / −10000 USD as balanced | Per-currency zero-sum in `JournalEntry`; regression test `JournalEntryTest`; GUARDRAIL TODO: DB CHECK constraint (B backlog) | 4a1c6ea
L-002 | 2026-06-09 | persistence/no-op-optimistic-lock | Bulk JPQL balance UPDATE checked `version` but never incremented it → lost updates undetectable | UPDATE now `SET version = version+1`; `@Modifying(clearAutomatically,flushAutomatically)` | 4a1c6ea
L-003 | 2026-06-09 | identifier-convention-drift | Two different account-id conventions (`la_merchant_recv_` vs slug) → every non-USD payment dead-lettered | Single canonical `EnsureAccountsExistUseCase.accountId()` + typed helpers used everywhere | 4a1c6ea
L-004 | 2026-06-09 | idempotency/wall-clock-key | Billing idempotency key embedded `System.currentTimeMillis()` → every retry a fresh key → double charges | Deterministic key per (invoice, logical attempt); GUARDRAIL TODO: lint for currentTimeMillis in idempotency keys | 4a1c6ea
L-005 | 2026-06-09 | fraud/score-polarity | Signifyd TRUST score (higher=safer) used directly as RISK score → approved fraud, blocked good orders | Inverted mapping with documented polarity; GUARDRAIL TODO: provider-scale unit tests (B-014) | 4a1c6ea
L-006 | 2026-06-09 | money/currency-exponent | FRM amount conversions hardcoded ÷100 / ×10000 → 10–100× wrong for JPY/BHD | `FrmAmounts` derives factor from `Currency.getDefaultFractionDigits()` | 4a1c6ea
L-007 | 2026-06-09 | security/non-constant-time-compare | Webhook HMAC compared with `String.equalsIgnoreCase` (timing side-channel) | `MessageDigest.isEqual`; GUARDRAIL TODO: semgrep rule for HMAC string compare | 4a1c6ea
L-008 | 2026-06-09 | security/path-traversal | Evidence storage joined attacker-controlled filename into a path | Sanitize each segment + `normalize().startsWith(root)` check | 4a1c6ea
L-009 | 2026-06-09 | pci/unkeyed-fingerprint | PAN fingerprint was unkeyed SHA-256 (brute-forceable low-entropy PAN) | HMAC-SHA256 keyed from master key; store 6-digit BIN not 8 | 4a1c6ea
L-010 | 2026-06-09 | security/cross-tenant-cache-key | Idempotency cache key was the raw client header → cross-merchant response leakage; 5xx cached 24h | Scope key per caller credential hash; never cache 5xx | 4a1c6ea
L-011 | 2026-06-09 | startup/component-scan-root | @SpringBootApplication under io.nexuspay.app hid sibling modules' entities + all Modulith modules | Moved to io.nexuspay root; ModulithVerificationTest now meaningful | 4a1c6ea
L-012 | 2026-06-09 | startup/unimplemented-port | Port interfaces injected into @Service beans with no implementation → context fails (reconciliation, dispute) | Implemented adapters; GUARDRAIL: a context-load smoke test would catch this class (IDEAS) | 4a1c6ea
L-013 | 2026-06-09 | startup/bean-cycle | DataSource-decorating @Bean that consumed a DataSource created a Flyway↔datasource cycle | Decorate via BeanPostProcessor (no new bean edge) | 4a1c6ea
L-014 | 2026-06-09 | outbox/ack-ordering | Relay marked events published before Kafka ack → events lost on broker outage | Await send ack before markPublished | 4a1c6ea

L-020 | 2026-06-10 | repo-health/clean-clone-unbuildable | A clean clone (CI/new contributor) could not build: gradlew lost its +x bit (exit 126) AND gradle-wrapper.jar was excluded by a `*.jar` gitignore (ClassNotFoundException GradleWrapperMain) | committed the wrapper jar (gitignore negation after *.jar) + git update-index --chmod=+x; GUARDRAIL: CI build job is the standing check (only CI caught both) | CI run on PR #1
L-021 | 2026-06-10 | deps/testcontainers-commons-compress | Integration tests (first CI run) died with ExceptionInInitializerError in commons-compress TarArchiveOutputStream — Testcontainers 1.19.8 needs commons-compress 1.24.0 but Boot 3.2.5 BOM pulls 1.25.0 | force 1.24.0 (test-only); lesson: pin TC's commons-compress when on a newer Boot BOM | PR #1
L-022 | 2026-06-10 | deps/flyway-major-vs-boot | AbstractMethodError in FlywayExecutor — catalog pinned Flyway 10.15.0 + flyway-database-postgresql (a Flyway-10 artifact, unresolvable under Boot 3.2.5's Flyway-9 BOM) run against Boot's Flyway-9 autoconfig | use Boot-managed flyway-core 9.22.3 (Postgres support built in), drop flyway-database-postgresql; GUARDRAIL: don't pin a major Flyway above what the Boot BOM supports | PR #1
- (root-cause class "deps/*" now 2× (L-021/22), "repo-health/*" 1× — all CI-surfaced; reinforces that pushing to CI is the verifier for packaging/dep issues local Windows can't catch)
L-019 | 2026-06-10 | money/minor-units-x-major-rate-no-exponent | FX convert multiplied raw ISO-4217 MINOR units by a MAJOR-unit rate with no exponent handling → USD→JPY 100× off, BHD 10× — found in 3 sites (FxRate, FxRateLock, CurrencyConversion.fxGainLoss); SAME class as L-006 (FrmAmounts hardcoded /100) → RECURRING, meta-review should add a systemic guardrail | Single exponent-aware `CurrencyMath` helper all sites delegate to + 11 tests; GUARDRAIL TODO: arch/grep test forbidding `.multiply(rate` on raw minor units | B-014a
L-018 | 2026-06-10 | money/maker-checker-not-execute-once | Approved refund executed with a null idempotency key via a read-then-write approve that ignored tenant → duplicate/cross-tenant refund under retry or concurrent approve | Atomic conditional UPDATE (WHERE status='PENDING' AND tenant_id=?, rows==1) + deterministic idempotency key from approval id + tenant-ownership check; ApprovalServiceTest/RefundOrchestrationServiceTest. Residual stuck-APPROVED recovery → B-022 | B-009
L-017 | 2026-06-10 | security/dev-default-secret-in-prod | Session HMAC key, webhook secret, vault master key defaulted to committed dev values with no prod guard — a missing env var would sign/encrypt with a public key | StartupSecretsValidator fail-fast under prod profile + application-production.yml forcing it + a drift-guard test asserting KNOWN_DEFAULTS stays in sync with application.yml (control can't fail open) | B-004
L-016 | 2026-06-10 | concurrency/lease-shorter-than-work | A fixed-TTL distributed lock over an unbounded long-running (≤500 PSP charges) job can expire mid-run → second replica starts → double-charge (a new invoice per cycle means no downstream idempotency saves it) | Lease renewal at ttl/3 while work runs + atomic owner-checked Lua release; SchedulerLockTest; caught by adversarial review, not initial tests | B-001/ADR-006
L-015 | 2026-06-10 | persistence/jsonb-as-varchar | `settlement_records.raw_data` jsonb column mapped as a plain String (no @JdbcTypeCode) → every ingest INSERT aborts; parser also stored a non-JSON CSV line | @JdbcTypeCode(SqlTypes.JSON) + parser emits valid JSON; StripeCsvParserTest; GUARDRAIL TODO: a grep/arch test for `columnDefinition="jsonb"` String fields lacking the annotation | B-010
- (root-cause class "persistence/*" now 2× with L-002 — meta-review watch)

RECURRING-CLASS WATCH (for meta-review): "startup/*" appears 3× (L-011/12/13) — a
CI context-load smoke test (no external infra, mocked) is the systemic guardrail.
"money/*" appears 3× (L-001/04/06) — mutation testing on ledger/billing/fraud is
the systemic guardrail (B-005/B-014).

## B-011 saga — the full integration-test bring-up (2026-06-10)
META-LESSON (the big one): the 13 integration tests had NEVER run since they were
written — Flyway's cross-module V1 collision aborted the schema before any test
could boot the context. So ~10 layers of schema↔entity drift and wiring bugs
accumulated UNVERIFIED behind that one blocker. Clearing it made CI surface them
one boot-layer at a time (resolver → discovery → DDL → validate → bean wiring →
external deps → security → assertions). GUARDRAIL (now standing): CI runs the full
context against real Postgres/Kafka/Redis (Testcontainers) and the integration
suite is GREEN — this is the systemic guard L-011/12/13 called for, now real. Any
future drift in this class fails CI immediately. Discipline reaffirmed: do NOT
delete load-bearing infra on an assumption (I deleted the 13 Flyway customizers
believing the base scan was redundant — it silently dropped 11 modules; L-013/§13).

L-023 | 2026-06-10 | db/flyway-location-semantics | (a) bare `classpath:db/migration` recursive scan is unreliable across separate module JARs; (b) a profile yaml `locations` list REPLACES (not merges) the base, so application-test.yml's hand-maintained 4-leaf list silently dropped the `app` leaf (current_tenant_id) → every RLS policy after it failed | explicit per-module LEAF locations + `fail-on-missing-locations: true`; test profile inherits the full list (no override) so test schema == prod | B-011
L-024 | 2026-06-10 | db/rls-policy-references-missing-column | RLS policies on child tables (split_rules, webhook_triggers) compared a `tenant_id` column the table doesn't have (tenant lives on the parent) → 42703 once migrations finally ran | derive tenant from the parent via correlated subquery; GUARDRAIL: a CI parser asserting every `USING (tenant_id` policy's table has the column | B-011
L-025 | 2026-06-10 | jpa/hibernate-validate-drift | ddl-auto=validate surfaced mass schema↔entity drift never caught before: Spring Modulith event_publication table+event_type missing (JPA variant ships no DDL); 23 jsonb String columns lacked @JdbcTypeCode(JSON) (also breaks INSERT bind); List/Map fields map as JSON not native array (events text[]→jsonb); Double↔DECIMAL | reconcile via the entity (javap for libs); a static entity↔migration type checker batches the whole class instead of 1-CI-cycle-each; GUARDRAIL: validate in CI is now the standing check | B-011
L-026 | 2026-06-10 | jpa/nested-repositories-not-scanned | 4 out-adapters declared Spring Data interfaces as NESTED types; default Boot scan uses considerNestedRepositories=false → NoSuchBeanDefinitionException, never seen because the context never booted | @EnableJpaRepositories(considerNestedRepositories=true) on the app class (defaults preserve EMF/TM + base package) | B-011
L-027 | 2026-06-10 | rls/set-command-no-bind-params | `SET LOCAL app.current_tenant_id = ?` as a PreparedStatement is a syntax error — PostgreSQL SET takes no bind parameters; broke every connection checkout with a tenant set | use `SELECT set_config('app.current_tenant_id', ?, true)`; deeper B-002 (set runs pre-transaction at getConnection; test user is RLS-exempt owner) still tracked | B-011/B-002
L-028 | 2026-06-10 | web/security-exception-as-500 | a @ControllerAdvice catch-all @ExceptionHandler(Exception) caught Spring Security's AccessDeniedException before ExceptionTranslationFilter → authorization failures leaked as 500 instead of 403 | explicit @ExceptionHandler(AccessDeniedException)->403; GUARDRAIL: integration tests asserting 403 for unauthorized roles (now green) | B-011
L-029 | 2026-06-10 | health/downstream-drags-aggregate | external-dependency health indicators (HyperSwitch PSP, Keycloak) as plain @Component connection-refuse with no backend → aggregate /actuator/health = DOWN (503) in any env lacking them | make such indicators @ConditionalOnEnabledHealthIndicator (default-on) so they're disableable; off in the test profile; (consider moving downstream checks to the readiness group in prod) | B-011
L-030 | 2026-06-10 | test/external-deps-unguarded | full-context integration tests tried to reach Vault (token), Keycloak, PSP that aren't in the container set; some were guarded (@ConditionalOnProperty Temporal/Vault-config) but others weren't | disable/guard every external integration in the test profile (spring.cloud.vault.enabled=false, health indicators off); pattern: external clients must be conditional so a no-backend env degrades cleanly | B-011
L-031 | 2026-06-10 | security/wiring-is-not-enforcement | Wiring a built-but-unwired control (B-003 fraud+sanctions gate) made it LOOK done, but the T3 review found it was bypassable: covered only 1 of ≥3 PSP entrypoints (not confirm/capture or billing/b2b/workflow callers), drove sanctions off client-supplied metadata an attacker can omit/forge, and sat on an OFAC adapter whose CSV parser silently produced an empty list (→ 4-country static fallback, fail-open). Recurring bug class: a control is only as strong as its WEAKEST entrypoint + its most-attacker-controlled INPUT + its failure DIRECTION (default-allow = broken sanctions). | GUARDRAIL: for any security gate, the review must enumerate (a) every entrypoint to the protected action and confirm each is gated, (b) whether each decision input is server-authoritative or client-controlled, (c) the fail direction (closed for sanctions). Don't overclaim coverage in commit/docs; an honest first-cut + tracked gaps (B-024..B-028, ADR-009) beats a "done" that's bypassable. | B-003
L-032 | 2026-06-10 | security/uniform-control-on-non-uniform-flows | Moving the fraud gate to the PaymentGatewayPort boundary (B-024) made it screen ALL callers uniformly — but a "REVIEW → hold capture" decision that is right for an INTERACTIVE checkout is WRONG for a server-initiated recurring billing charge: holding it returns requires_capture, which billing reads as a payment failure and DUNS the customer (a live regression). T3 review (M1) caught it. Lesson: when one control spans heterogeneous flows (interactive vs pre-authorized/recurring vs batch), the ENFORCEMENT ACTION must be flow-aware, not just the decision — same screen, different consequence. | GUARDRAIL: hold-capture is INTERACTIVE-only; server rails capture + flag for analyst review (sanctions still hard-block). When a port-boundary decorator changes behavior for a previously-unscreened caller, trace that caller's success/failure interpretation (here billing's PaymentResult→boolean→dunning) before shipping. | B-024
L-033 | 2026-06-13 | test/new-profile-trips-prod-secrets-guard | The RlsEnforceIT (which activates the `rls-enforce` Spring profile) failed to boot in CI: StartupSecretsValidator (B-004) refused to start because it classifies ANY unrecognized non-dev profile as production (fail-safe), so the built-in DEV default secrets were "in effect under a production profile". The GUC mechanism under test was never the failure — the context never finished booting. | GUARDRAIL: when a test (or any runtime) activates a NON-dev profile, it inherits production-grade guards. Fix the TEST (supply non-default managed secrets via @DynamicPropertySource — jwt ≥32 bytes for HMAC, valid-Base64 vault key, non-default webhook secret), NEVER weaken the control or bake test secrets into the shared profile yaml (that profile is also the real prod-cutover profile and must keep failing closed). Read a failing security guard's exact trigger before "fixing" it. | B-002/B-004
L-034 | 2026-06-13 | aop/role-pin-is-threadlocal-not-tx | For cross-tenant jobs, @SystemTransactional is acted on by an @Around aspect that wraps the call in DbRoleContext.runAs(SYSTEM,…) — a CALL-SCOPED thread-local the routing datasource reads on every getConnection. Two consequences that drive WHERE you annotate: (a) annotate the JOB ENTRY method (the @Scheduled tick); the pin then covers the whole SYNCHRONOUS subtree — nested @Transactional service calls, self-invocation, and even non-transactional reads all route to the system pool — so you need NOT chase the inner tx boundary; (b) it does NOT cross thread boundaries: async callbacks (e.g. Kafka producer whenComplete), @Async, and executor work run on the DEFAULT (APP) role and would fail-closed if they touch RLS tables. | GUARDRAIL: put @SystemTransactional on the entry method; for any work the job hands to another thread, either keep it off RLS tables or give it its own SYSTEM-pinned path. Verify the aspect order pins SYSTEM (HIGHEST_PRECEDENCE) before the TransactionInterceptor (LOWEST). | B-002
L-035 | 2026-06-13 | security/system-bypass-forfeits-with-check | Classifying a cross-tenant background job as SYSTEM (BYPASSRLS) is correct for the all-tenant DISCOVERY scan (a per-tenant loop can't see which tenants have due work), but BYPASSRLS also forfeits the WITH CHECK on the per-item WRITES — a poisoned/aliased row could persist a child (invoice line / outbox event / dunning attempt) under the wrong tenant with no DB backstop. The adversarial pass flagged this on RenewalScheduler + TrialManagementService; the post-ship review then settled BOTH the same way — annotate the discovery sweep @SystemTransactional (TrialExpirationScheduler was initially missed → fixed, making 16 SYSTEM jobs), with per-item tenant binding on the writes deferred to B-002-activation-tenant. Two independent agents gave structurally-identical code different verdicts (SYSTEM vs TENANT_SCOPED) — a reminder to reconcile sibling-job classifications explicitly. | GUARDRAIL: for an all-tenant sweep, run the DISCOVERY read as SYSTEM but bind TenantContext to each row's tenant for its per-item write transaction (split the one cross-tenant tx into per-tenant txns; one app-role tx GUC is tx-local and single-valued). A Kafka consumer carrying a tenantId is always TENANT_SCOPED, never SYSTEM. Tracked as B-002-activation-tenant. | B-002
L-036 | 2026-06-14 | tx/helper-tx-downgrades-inner-isolation | A generic "run this in a tenant-bound REQUIRES_NEW transaction" helper (TenantWorkRunner.runInTenant, default isolation) SILENTLY downgrades an inner @Transactional(SERIALIZABLE) method that JOINS it (PROPAGATION_REQUIRED): Spring does not escalate an existing tx's isolation and, with validateExistingTransaction=false (the default), does not even error — so the ledger's SERIALIZABLE journal write quietly ran at READ_COMMITTED. Worse, this happened in BOTH modes, so it was also a dormancy break at enforce=false. | GUARDRAIL: when the inner work already manages its own transaction(s)/isolation, BIND the tenant WITHOUT opening an enclosing tx (added TenantWorkRunner.bindTenant — set APP role + TenantContext on the thread, let the inner @Transactional open its own tx at its own isolation). Only use runInTenant/callInTenant when YOU own the boundary (handlers that aren't themselves transactional, or per-item sweep writes). Audit every helper-wrapped call for an inner non-default isolation. | B-002-activation-tenant
L-037 | 2026-06-14 | rls/app-level-filter-is-not-gated-by-the-guc | Swapping a repository finder (findAllByEnabledTrue → findAllByTenantIdAndEnabledTrue) to "add RLS scoping" added an UNCONDITIONAL SQL `WHERE tenant_id=?`. That filter is application-level — it is NOT gated by rls.enforce or the GUC — so it changed behavior at enforce=false too: the event tenant resolves to "default" (no tenant on the wire pre-cutover) while endpoints are stored under real tenants → zero matches → webhooks silently stopped in the DEFAULT config. The dormancy guarantee only covers the GUC/role machinery (the inline helper ignores the tenant); it does NOT cover app-level query changes. | GUARDRAIL: a behavior change that must be inert until cutover has to be gated on the enforce FLAG (inject nexuspay.multi-tenancy.rls.enforce and branch), not merely left to the dormant GUC. Any query/method swap is a behavior change — prove the enforce=false path is byte-identical (a regression test or explicit branch). Caught by the adversarial review, not by the bean-wiring dormancy IT. | B-002-activation-tenant
L-038 | 2026-06-14 | ci/which-workflow-is-green | I reported "CI green" for branch commits when only perpetua-gates (changelog/secrets/ratchet) had run — the build+test CI workflow (ci.yml) triggers on push:[main] + pull_request:[main], so branch pushes only got build+test while PR #1 was OPEN. Merging PR #1 silently stopped branch build+test; the next pushed increments were never compiled or IT-run, yet a green perpetua-gates looked like success. | GUARDRAIL: when claiming CI green, confirm WHICH workflow(s) ran (`gh run list` → check the name is the build+test one), not just that "a run" is green. Process fix applied: ci.yml now also runs on push:[perpetua/**] so branch pushes are always compiled+IT-run regardless of PR state. Report verification faithfully — name the workflow. | meta
L-039 | 2026-06-14 | tx/durable-error-record-erased-by-outer-rollback | The B-015 fix persisted a PARSE_ERROR exception for each bad settlement row so money never vanishes silently — BUT it saved them inside the caller's @Transactional (REQUIRES default) boundary (runFromUpload). When anything downstream (matching, a DB error) throws, the orchestrator marks the SHARED transaction rollback-only and re-throws, so the just-persisted error records roll back too — re-creating the exact silent-vanish defect, relocated from the parser to the tx boundary. Unit tests with a MOCKED repo did NOT catch it (a Mockito interaction is recorded regardless of rollback). Caught by adversarial review tracing the real wired flow. | GUARDRAIL: a record whose PURPOSE is to survive a failure of the surrounding work (audit log, dead-letter, parse/validation exception, reconciliation exception) MUST be written in its OWN committed transaction — @Transactional(propagation = REQUIRES_NEW) on a SEPARATE bean (not self-invoked) — never in the same tx as the work that may roll back. When testing durability, exercise the real tx boundary (a rollback after the write), not a mock that ignores rollback. | B-015
L-040 | 2026-06-14 | compliance/verify-the-real-external-feed-and-never-call-it-in-tests | The B-026 OFAC parser was implemented against an ASSUMED feed shape (a `countries` column of full names) that does not exist; the real Consolidated Screening List is 29 columns with ISO-2 encoded at the tail of each `;`-separated `addresses` entry. The implementation even shipped a FABRICATED test fixture matching the wrong shape, so the whole parser suite passed GREEN while validating a contract production never sees — the most dangerous failure mode for a compliance control. Separately, the refresh-failure tests used a hardcoded absolute feed URL that overrode the test's baseUrl, so they made a LIVE network call to data.trade.gov and passed/failed by CI internet availability. Both caught by adversarial review (which pulled the live 25,766-row feed) + CI. | GUARDRAIL: for any external-feed/3rd-party-format parser (esp. compliance), VERIFY the real feed's actual schema (pull a live sample) and test against a faithful captured excerpt, not a hand-invented one; make the feed URL/endpoint injectable so unit tests NEVER hit the network; assert the parser does NOT throw on the real header and yields the expected set. A green suite over a fabricated fixture is worse than no test. | B-026
L-041 | 2026-06-14 | jpa/save-defers-insert-past-the-catch + migration/unique-index-needs-prededup | A race backstop that relies on repository.save() throwing DataIntegrityViolationException inside a try/catch FAILS against a real DB: an entity with a pre-assigned @Id (no @GeneratedValue/@Version, not Persistable) makes Spring Data save() do em.merge(), which DEFERS the INSERT to flush/commit — so the unique-constraint violation surfaces OUTSIDE the catch (at the next auto-flush or commit), the loser's exception propagates, and a duplicate side-effect (event) may fire first. A unit test that stubs save() to throw is GREEN but proves nothing (it encodes the false synchronous-save assumption). SEPARATELY: a `CREATE UNIQUE INDEX` migration BRICKS BOOT on any baseline-on-migrate DB that already holds duplicate rows (IF NOT EXISTS guards the index, not the data) — Flyway aborts the whole shared migration set. | GUARDRAIL: for a synchronous DB-constraint check, use saveAndFlush (or make the entity Persistable) so the violation is raised at the call inside the catch; test it with the FLUSHING method stubbed and, where infra exists, a real @DataJpaTest. For any unique-index migration, DELETE pre-existing duplicates (keep one per group via a strict total-order predicate) BEFORE creating the index, and verify on a non-empty DB. | B-029/B-027b
L-042 | 2026-06-14 | test/agent-generated-tests-need-CI-as-the-compile-run-loop | B-014 added ~272 agent-written tests across 4 modules; with NO local Gradle (sandbox loopback), they could only be validated by CI, which surfaced bugs ONE module at a time over 7 iterations. Recurring agent-test bug classes (none catchable by inspection alone): (1) `List.of(singleObjectArray)` SPREADS the array into List<Object> via varargs instead of List<Object[]> — use the `List.<Object[]>of(...)` type witness; (2) `assertThat(Optional).doesNotContain(...)` — OptionalAssert has no such method (use hasValueSatisfying); (3) Mockito 5 varargs stub needs matcher-arity == actual vararg count (a single any() matches only a 1-element call, so the stub silently doesn't match and the asserted branch never runs); (4) com.sun.net.httpserver.Headers normalizes header names to First-char-upper/rest-lower (X-NexusPay-Sig -> X-nexuspay-sig); (5) a unit test stubbing a feed/HTTP client with a hardcoded ABSOLUTE url makes a LIVE network call on CI (make the url injectable); (6) save()-with-pre-assigned-@Id defers the INSERT past a try/catch (L-041). META: an inspection-only "audit" agent missed the runtime + generics issues; CI is the only real validator. The aggressive testing ALSO found a REAL production bug — ledger PaymentEventConsumer NPE'd on switch(null) for a missing event_type (-> retry -> DLT instead of graceful ignore), now guarded — vindicating the test-aggressively mandate. | GUARDRAIL: when generating tests without a local build, budget for several CI cycles; have the implementer trace each assertion through REAL runtime behavior and double-check varargs/generics/overload-resolution/header-casing; never let a unit test hit the network; and treat a test that won't pass as possibly a real bug (fix production, don't weaken the test) — here it was. | B-014
L-043 | 2026-06-14 | ci/report-only-scan-can-still-red-the-build-two-ways | Wiring a NEW security scanner "report-only first" (§15.3) means the SCAN's own non-zero exit is swallowed (`semgrep ... || echo "::warning::..."`) — but two adjacent mechanics can still turn the job RED and defeat report-only: (1) a SEPARATE `actions/upload-artifact@v4` step FAILS when its `path:` matches no file ("No files were found"), and `if: always()` only forces the step to RUN, it does NOT make a missing-file fatal-error non-fatal — so if the scanner CRASHES before writing its SARIF (registry/rule-fetch failure, core error), the upload reds the build even though the scan was swallowed; (2) the swallow itself is INDISCRIMINATE — `|| echo` eats ANY non-zero exit, so a fully BROKEN scan (registry unreachable, 0 rules, all files unparsed) also exits non-zero and is swallowed, showing GREEN-but-scanned-nothing; a "0 findings" pre-triage then can't be told apart from "scanned nothing" when the gate is later flipped to blocking. Both caught by the CI-correctness review dimension, not by running it. | GUARDRAIL: for a report-only scan, pre-create the output file (`: > out.sarif`) AND set `if-no-files-found: warn` on the upload so a scanner crash degrades to a warning, never a red build. When later FLIPPING report-only→blocking, do NOT just drop the `|| echo`: branch on the tool's exit codes (semgrep 0=clean / 1=findings / >=2=error) to fail on real findings AND on a broken scan but never on neither, and confirm the runner actually scanned a non-zero file/rule count before trusting a 0-finding result. | B-006
L-045 | 2026-06-14 | ci/exit-code-branch-is-dead-under-bash-e | A CI step that runs a tool then branches on its exit code (`tool ...; rc=$?; if [ $rc -eq 1 ]...`) is DEAD CODE under GitHub Actions' default `run:` shell `bash --noprofile --norc -eo pipefail`: `-e` aborts the step the instant the tool exits non-zero, BEFORE `rc=$?` and the if/elif/else ever execute. The OUTCOME can still be accidentally correct (the step fails on any non-zero), but the intended diagnostics never print and you cannot DISTINGUISH cases (e.g. semgrep exit 1 findings vs >=2 broken-scan) — and a comment claiming "rc reflects the exit" is then false. Caught by the CI-correctness reviewer simulating the exact `-e` shell. | GUARDRAIL: to actually capture an exit code and branch under the default shell, neutralize -e around just that command: `set +e; tool ...; rc=$?; set -e` (or `if tool ...; then` / `tool ... || rc=$?`). Whenever a `run:` block captures `$?` or means to branch on it, confirm the shell isn't `-e` (or wrap it) — otherwise the branch is unreachable on the failure paths you care about. | B-006/B-012
L-047 | 2026-06-15 | security/cross-origin-iframe-handshake-must-use-browser-attested-origin | The PCI card iframe (checkout-sdk) protects the raw PAN from the merchant page via cross-origin isolation; its ONLY leak channel is the postMessage handshake that sets apiBase/sessionToken. Getting that handshake's origin trust wrong = PAN exfiltration. Three iterations of adversarial review were needed to get it right: (1) original code posted with target origin '*' and the receivers ignored event.origin entirely (forge a STYLE_UPDATE -> set apiBase=evil -> PAN POSTed to attacker); (2) first fix added origin checks but pinned the expected parent from the SENDER-DECLARED payload.parentOrigin (spoofable) and had an accept-all `!state.parentOrigin` clause that fails OPEN when the referrer is stripped; (3) second fix pinned to the browser-attested event.origin via trust-on-first-use (TOFU) — better, but a cross-origin co-resident frame (evil.com embedded on the merchant page) that posts FIRST still wins the pin. The correct primitive is window.location.ancestorOrigins[0] — the browser-ATTESTED immediate-parent origin, not spoofable and known at init with no race; it must be the SOLE accepted parent origin where available (Chromium/WebKit), with TOFU-on-event.origin only as the Firefox fallback. | GUARDRAIL: for ANY cross-origin iframe message channel that carries security-relevant config (esp. a PCI/PAN boundary): SEND to an exact computed target origin, never '*'; RECEIVE-check event.origin BEFORE processing on EVERY listener (the 3DS challenge listener had none — any window could force CHALLENGE_COMPLETE); derive the expected counterpart origin from a BROWSER-ATTESTED source (ancestorOrigins for the parent; new URL(src/apiBase).origin for the child) — NEVER from a value the sender puts in the message payload; treat TOFU as a last-resort fallback, not the primary check; and validate any nextAction/redirect URL is https on an allowed host before navigating/iframing. There is no origin-based defense against a SAME-origin hostile script on the parent page — that is the merchant's page-integrity responsibility. | B-006/B-021/DX-1
L-049 | 2026-06-15 | test/red-team-tests-must-fail-on-vulnerable-code + gradle-tag-filter-inheritance | Building an "aggressive simulation/red-team" suite, the adversarial reviews caught TWO classes of defect that would have shipped a false-assurance security harness. (1) FALSE-ASSURANCE RED-TEAM TESTS: a security regression test is only meaningful if it FAILS on the current vulnerable code and PASSES once the fix lands. Several did the opposite — PanPersistenceRedteamTest attacked vaulted_cards.encrypted_pan (already real AES-256-GCM) so it was green on main while the ACTUAL audit hole (raw PAN base64 in payment_tokens.token_data via the gateway tokenize path) went untested; IdempotencyReuseRedteamTest "passed" only because no PSP is wired in tests so both calls 422'd and an error-id fallback made them compare equal; HyperSwitchWebhookOrderingRedteamTest asserted `status != 208` against a handler that uses 200 for dedup (and 401s unsigned first) — meaningless; three IDOR tests used victim ids that were never SEEDED, so they failed on main as "unknown id → 500" not as a proven cross-tenant leak (a not-found→404 fix would green them without proving isolation). FIX DISCIPLINE: retarget to the real vulnerable path so it fails-on-main (PAN); SEED a real victim resource owned by the other tenant + assert attacker→404 WHILE a same-tenant control read→200 (IDOR); or @Disabled("<why> — enable when SEC-X lands") with a precise TODO when the harness can't yet demonstrate the attack (needs a stub PSP) — NEVER assert the insecure/non-existent behavior. (2) GRADLE TAG-FILTER INHERITANCE: a root `subprojects { tasks.withType<Test> { useJUnitPlatform { excludeTags("redteam") } } }` ALSO configures a later `tasks.register<Test>("redteamTest") { useJUnitPlatform { includeTags("redteam") } }` (withType matches future tasks; useJUnitPlatform MUTATES the same JUnitPlatformOptions, not a fresh one) → the task ends up include AND exclude the same tag → JUnit's exclude wins → the report-only job silently selects ZERO tests. Reset the inherited filter in the dedicated task: `useJUnitPlatform { excludeTags = emptySet(); includeTags = setOf("redteam","simulation") }`. | GUARDRAIL: for any security/red-team test, prove it fails on the vulnerable baseline (or @Disable with a reason) — a green-on-vulnerable test is worse than none. Keep red-team tests @Tag-excluded from the default gate (report-only via a continue-on-error job, §15.3) so they can't red main while the fixes are un-merged, and flip each into the gate as its SEC-* fix lands. When a dedicated Gradle Test task needs DIFFERENT tag filters than the root applies to all Test tasks, reset excludeTags in that task or the root's exclude silently wins. | simulation-redteam
L-048 | 2026-06-15 | modulith/shared-cross-cutting-contracts-go-in-common-not-copied-from-iam | The audit's prescribed tenant-authority fix ("resolve tenant from @AuthenticationPrincipal NexusPayPrincipal, mirror gateway-api") was UNCOMPILABLE in marketplace/vault/b2b/fraud: NexusPayPrincipal + TenantContext live in `iam`, and those modules depend ONLY on `common` (auto-detected CLOSED Modulith modules with no package-info → default deps = common; fraud explicitly pins allowedDependencies={common}). gateway-api's pattern works only because gateway-api depends on :iam — it is NOT a copyable template. A naive copy would either fail ModulithVerificationTest (illegal new dep edge) or fail to compile (type off the classpath). The fix: route the cross-cutting contract through `common` (the @Modulithic sharedModule, OPEN to all) — exactly mirroring the existing TenantWorkRunner/@SystemTransactional precedent (a common-side interface/contract whose richer impl lives in app, so any module can depend on it). Concretely: a common TenantPrincipal interface that the iam NexusPayPrincipal implements (iam→common is the legal direction), a common CallerTenant.require() reading SecurityContextHolder, and a common TenantOwnership helper → common ResourceNotFoundException. Also surfaced a latent bug while wiring the 404: services threw IllegalArgumentException for not-found but GlobalExceptionHandler had no handler for it (list ended at NexusPayException→500), so every not-found was already a 500 — the new ResourceNotFoundException→404 fixes both the cross-tenant 404 AND the pre-existing 500. | GUARDRAIL: before "apply pattern X from module A to modules B,C,D", CHECK the Modulith dependency graph (build.gradle.kts deps + package-info allowedDependencies + which module owns the types X references). If X references a type owned by a module B,C,D don't depend on, the contract must be lifted into `common` (interface in common, impl/principal implements it from the owning side) — never add a new cross-module dep edge to make a copy compile. Use 404 (not 403) for cross-tenant resource access to avoid an existence oracle, and collapse absent+wrong-tenant into one not-found path. | SEC-BATCH-1
L-044 | 2026-06-14 | test/property-tests-can-be-vacuous-mutation-test-them | The B-029-hardening fraud fingerprint shipped property/fuzz tests that NAMED the right invariant (canonicalization is injective ⇒ no delimiter-injection collision) but did NOT actually test it: the T3 test-adequacy reviewer MUTATION-TESTED the suite — replaced the injective length-prefixed canonicalize() with the exact naive pipe-join the design warns against — and ALL 22 tests stayed GREEN. The chosen "anti-collision" tuples (pipe-shift "1|US|a", field-boundary shifts, a 4000-iter fuzz whose tupleKey was itself length-prefixed) all happened to stay distinct even under the vulnerable join, so none would FAIL if the security boundary regressed. A concrete forgeable collision existed under the mutant: (1,"US","a|b","tok") vs (1,"US|a","b","tok") → identical fingerprint = the precise reused-key-different-charge attack the feature exists to stop. The implementation was correct; the TESTS were the defect (worse than no test — they broadcast false assurance on a money/security control). Fixed with real delimiter-SHIFT collision pairs that collide ONLY under a pipe-join (asserting both fingerprint!= AND canonicalize() bytes !=), an extended fuzz that emits cross-field shift variants, and self-verification by the same mutation (pass-on-real / fail-on-mutant). | GUARDRAIL: a property/anti-collision/security test is only real if it FAILS when the property is broken — verify that by mutation: temporarily swap in the naive/vulnerable implementation and confirm the test goes RED. Choose adversarial inputs that actually exercise the boundary (here: tuples that ALIAS under the form you're defending against), not inputs that merely look varied. For any T3 canonicalization/parser/crypto surface, include at least one input pair that is a known collision under the rejected-but-tempting wrong implementation. | B-029-hardening
L-046 | 2026-06-14 | flyway/two-in-flight-PRs-both-grab-the-next-version | When two migration-bearing changes are developed CONCURRENTLY on separate branches, each (and each workflow that investigates "the next free GLOBAL Flyway version") sees only ITS OWN tree — so both grab the same number. The B-022 reconciler workflow ran on a branch where the fraud-fingerprint PR's fraud/V4024 was absent, correctly computed the in-tree max as V4023, and picked V4024 — colliding with the open fraud PR's V4024. Two V4024 in the ONE shared global Flyway history (out-of-order DISABLED, all module leaves merged — B-011/L-023) = a boot-time "duplicate version" failure when both merge. CI did NOT catch it (each branch has only its own migration; a fresh-DB CI run is green per-branch). The same hazard applies to any append-only / numbered shared artifact: ADR numbers (ADR-017 was taken by the fraud PR), LESSON numbers (L-044), changelog entries — concurrent branches collide on the number AND conflict at the EOF append point. | GUARDRAIL: when more than one migration/ADR/lesson-bearing change is in flight at once, allocate versions/numbers GLOBALLY across the open set (don't just read your own branch) — reserve a distinct number per branch (fraud=V4024, B-022=V4025, leaving an intentional documented gap), and because out-of-order is disabled, MERGE in ASCENDING version order on any persistent DB (CI's fresh DB hides the ordering bug — it only bites prod/staging). State the merge-order dependency in the PR. A single agent can't see sibling branches; the human (or the orchestrator holding all branches) must deconflict. | B-022/B-029-hardening
L-050 | 2026-06-15 | css/rgba-cannot-wrap-a-css-variable-color-use-color-mix | The checkout widget's focus ring + tab tints were written `rgba(var(--nxp-color-primary, #hex), 0.15)` — INVALID CSS: rgba()/hsl() take numeric channels, NOT a CSS custom property (or a hex) as the color argument, so the whole declaration is dropped and the focus ring rendered NOTHING (no halo at all). It looked fine in code review and even compiled (it's a runtime CSS parse failure, not a build error); only a computed-style check in a real browser revealed `box-shadow: none`. This is the single biggest "looks cheap / broken" tell on a payment widget (the focus ring is the premium/cheap divider) and it was invisible to every non-rendering check. The fix: `color-mix(in srgb, var(--nxp-color-primary) 18%, transparent)` — the modern, token-friendly way to make a translucent shade of a variable color (the PCI iframe side was already correct, which is how the asymmetry was spotted). Surfaced by the premium-widget deep research + proven by live-Chromium computed styles. | GUARDRAIL: to make a translucent tint of a token/variable color, use color-mix(in srgb, var(--token) N%, transparent) — NEVER rgba(var(--token), a) or hsl(var(--token), …) (the var cannot be a single color channel set). For UI that styling can silently drop (focus rings, translucent overlays, token-derived colors), verify with a REAL browser computed-style check (or a Vite preview screenshot), not code review — a dropped CSS declaration passes build + unit tests. A theme-gen unit test asserting the generated CSS contains color-mix and NOT `rgba(var(` locks it. | premium-widget |
L-051 | 2026-06-15 | security/new-dev-default-secret-must-register-in-the-prod-boot-guard | SEC-BATCH-2 added HMAC verification to the dispute webhook with a NEW secret `nexuspay.dispute.webhook-secret` shipping a source-controlled dev default (`dispute_webhook_secret_for_local`) — but did NOT add it to StartupSecretsValidator.KNOWN_DEFAULTS (B-004's prod boot-guard). Net effect: a prod deploy that forgets the env var boots cleanly with a PUBLIC, repo-committed HMAC key → an attacker reads the secret from git, signs a forged dispute.opened, and the brand-new fail-closed gate PASSES it → forged chargeback-ledger postings. The fix that adds the auth control silently re-opened the hole one layer down, exactly the recurring env-override/secret class (L-017/L-033/D93). The fail-closed `blank→401` branch did NOT catch it because the default is NON-blank. Caught by the adversarial money-safety review, not by the auth-correctness review (which only checked the HMAC mechanics). | GUARDRAIL: whenever you introduce a NEW secret/credential with a dev default, in the SAME change register it in the prod secret-leak guard (KNOWN_DEFAULTS) + its env var in the validator message + the production yaml's required list + the drift-guard test — a new HMAC/signing secret is only as strong as the guarantee prod isn't running the public default. Mirror the existing secret's guard coverage, not just its mechanics. | SEC-BATCH-2 |
L-052 | 2026-06-15 | pci/base64-is-not-encryption-encrypt-pan-at-rest-behind-the-master-key | The SDK tokenize path persisted the PAN as base64 in payment_tokens.token_data with encryption_key_id NULL — base64 is an ENCODING, not encryption, so the column was a plaintext PAN at rest (a PCI-DSS violation; trivially `base64 -d` from any DB dump/SQLi/backup). It looked "tokenized" (opaque blob + a token id) but the card data itself was recoverable. The vault module already did it correctly (AES-256-GCM), but gateway-api couldn't reach the EncryptionPort. Fix: encrypt token_data via AES-256-GCM + a non-null encryption_key_id, with the contract lifted into :common (L-048) so gateway-api injects it with no gateway→vault Modulith edge (the single adapter bean resolves at the :app root). Existing base64 rows had to be PURGED (a data migration) — cleartext PAN at rest can't be re-encrypted in SQL (no key) and must not be left recoverable. | GUARDRAIL: for ANY stored cardholder/sensitive datum, "encoded/opaque" is NOT "protected" — assert it is AES-GCM ciphertext keyed by the master key (encryption_key_id set), non-reversible without the key; a token id alongside a base64/plaintext blob is still a breach. When a sibling module already encrypts, REUSE its EncryptionPort via :common rather than re-implementing or sending cleartext across a module boundary. A red-team test must base64-decode the persisted value and assert it is NOT the PAN (and would fail on the old behavior). When fixing PAN-at-rest, migrate/purge the legacy plaintext rows in the same change. | SEC-BATCH-3 |
L-053 | 2026-06-15 | security/ssrf-validate-is-not-enough-pin-the-ip-and-kill-redirects | Adding an SSRF allow/deny URL validator to the outbound webhook path looked sufficient but the adversarial review found it was bypassable THREE ways the validator alone can't stop: (1) HTTP REDIRECTS — the delivery client followed 3xx by default, so a merchant endpoint returns `Location: http://169.254.169.254/...` and the server fetches it with ZERO DNS race (the cleanest SSRF); (2) DNS REBINDING TOCTOU — validating the host then letting the HTTP client RE-RESOLVE at connect means the check and the fetch use independent DNS lookups, so a host that resolved public at validation rebinds to a private IP at connect; the first impl even RETURNED the validated InetAddress[] but the client discarded it; (3) registration-only validation is moot once either of the above applies at delivery. | GUARDRAIL: for server-side fetch of any caller-supplied URL (webhooks, feeds, image-by-URL, SSO metadata): (a) https-only scheme allowlist; (b) resolve ALL A/AAAA and deny loopback/RFC1918/link-local(incl 169.254.169.254)/ULA fc00::/7/CGNAT 100.64/10/IPv4-mapped/multicast/0.0.0.0/NXDOMAIN, rejecting if ANY record is private; (c) DISABLE redirect following (treat 3xx as a failure) — or re-validate every hop; (d) PIN the connection to the validated IP (resolve→validate→connect-to-that-IP via a custom DnsResolver, e.g. Apache HttpClient5, fail-closed for unpinned hosts; keep TLS SNI/Host = hostname so the cert validates) so the bytes go to the IP you checked; (e) validate at BOTH registration AND delivery. Unit-test the validator directly (encodings, IPv4-mapped, multi-record, NXDOMAIN) — it's the SSRF brain. | SEC-BATCH-4b |
L-054 | 2026-06-16 | spring/two-non-autowired-constructors-collapse-the-whole-context | SEC-4b added a second (package-private) test-SEAM constructor to WebhookDeliveryService (a @Component) alongside the production one, with NEITHER annotated @Autowired. Spring 6 with multiple candidate constructors and none annotated cannot choose — it falls back to a no-arg constructor that doesn't exist → BeanInstantiationException / NoSuchMethodException at context load → EVERY @SpringBootTest integration test fails at once (40+ here) with ONE shared root cause. It COMPILED fine and the unit test passed because the unit test calls the seam constructor DIRECTLY (no Spring), so the ambiguity never surfaced off-CI; the no-CI adversarial compile-correctness lens structurally could not catch it (only the full Spring context exercises autowiring). This is the exact bug class that vindicated holding merges for CI — restoring CI (public repo) caught it within minutes. | GUARDRAIL: a Spring bean (@Component/@Service/@Repository/etc.) must have exactly ONE injectable constructor, OR mark the intended one @Autowired — adding a SECOND ctor (even package-private, even "just for tests") REQUIRES @Autowired on the production ctor. Diagnostic signature: a wall of @SpringBootTest failures whose DEEPEST cause is BeanInstantiationException→NoSuchMethodException = ONE un-resolvable constructor, not N independent test bugs — hunt for a bean with 2 ctors. Prefer a static test factory/builder over a second injectable ctor. A green unit test that bypasses Spring is NOT evidence the bean wires. | SEC-BATCH-4b/CI |
L-055 | 2026-06-16 | test/registration-ssrf-gate-does-live-dns-use-a-resolvable-public-host | The SEC-14 @SafeWebhookUrl registration gate does REAL DNS resolution (anti-SSRF) and rejects NXDOMAIN. Three pre-existing webhook-endpoint ITs predated the gate and registered non-resolvable hosts — merchant.example.com / a.example.com (example.com has NO wildcard, so arbitrary subdomains are NXDOMAIN) and test.com — so registration now 400s instead of 201/403, failing tests that were CORRECT before the control existed. Fix: use https://example.com (the IANA-reserved domain that DOES resolve, the same host OutboundWebhookSsrfRedteamTest's 'accepted (201)' case uses) — never a made-up subdomain. | GUARDRAIL: when a security control that does live resolution/validation lands, AUDIT every existing test that exercises the now-guarded input — a control added in batch N silently breaks fixtures written in batch <N that look unrelated. For URL-validation ITs use a guaranteed-resolvable public host (example.com / www.example.com) for accept cases and reserve example.com SUBDOMAINS for NXDOMAIN-reject cases. CI (real DNS) is the oracle; these may not fail locally where the validator is mocked. | SEC-BATCH-4b/CI |
L-056 | 2026-06-16 | jpa/postgres-double-colon-cast-collides-with-hibernate-named-param | SEC-25's findStuckProcessing native @Query used the Postgres cast `'epoch'::timestamptz`. Hibernate 6 parses `:name` as a named parameter, and the `::` in `::timestamptz` collides with that prefix → Hibernate misparses it (tries to bind a param `timestamptz`) → the query throws SQLGrammarException/PSQLException at RUNTIME (every call site, all 3 reconciler ITs). It COMPILED fine and looked correct in code review + the 4 adversarial reviewers' static pass — only the real-Postgres CI surfaced it (Gradle can't run in the sandbox, so a native-query grammar bug is invisible until CI). Fix: use ANSI `CAST('epoch' AS timestamptz)` instead of `'epoch'::timestamptz`. | GUARDRAIL: in Hibernate native @Query strings NEVER use the Postgres `::type` cast operator — it collides with the `:param` named-parameter syntax. Use `CAST(expr AS type)` always. Grep new native queries for `'..'::` / `)::` before merge. This class of bug (and any native-SQL grammar error) is undetectable without a real-DB run — rely on CI, not static review, to gate native queries. | SEC-25/CI |
