/**
 * Payment Analytics Platform — consumes payment events, aggregates into
 * time-series rollup tables, and exposes API endpoints for authorization
 * rate analysis, PSP health scoring, revenue analytics, and decline analysis.
 *
 * @since 0.3.0 (Sprint 3.6)
 */
package io.nexuspay.analytics;
